# Optional: join the network your relay already uses.
# docker compose -f docker-compose.yml -f docker-compose.external-network.yml up -d
services:
  mistral-sticky:
    ports: []
    networks:
      - llm-net

networks:
  llm-net:
    external: true
    name: llm-net
