# Oracle ARM: docker compose -f docker-compose.yml -f docker-compose.server.yml up -d
services:
  mistral-sticky:
    ports: []
    networks:
      - 1panel-network

networks:
  1panel-network:
    external: true
