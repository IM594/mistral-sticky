# Security

This repository is public. Never open an issue, PR, or discussion that contains a real Mistral API key, `PROXY_TOKEN`, `.env`, `keys.txt`, or cooldown payload.

Report secret leaks privately. Do not paste keys into GitHub.
