# Real secrets — never commit
keys.txt
keys.json
cooldown.json
.env
.env.*
!.env.example

# Runtime data (keys, cooldown) — server only
/data/

# Local binaries / coverage
/mistral-sticky
/bin/
coverage.out
*.exe
*.test
.DS_Store
